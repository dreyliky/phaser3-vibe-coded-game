# Workflow

- **Assets**: Put in `public/assets/` (sprites, audio, etc.). Load in Boot scene.
- **Clean Code**: Remove unused imports/vars. Maintain consistent formatting. Maximally extract magic values into global constants.
- **UI Development**: When needing a UI element, first check if it exists. If not, create a shared, flexible, and scalable UI component first, then use it.
